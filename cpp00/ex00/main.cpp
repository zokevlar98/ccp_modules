#include <iostream>

int	main()
{
	long x;
	int y;

	std::cout<<"Enter tow number sperate with space: ";
	std::cin>> x >> y;
	std::cout<<"You numbers is : "<< x << " and " << y <<std::endl;
	// std::cout<<"number * 2 : "<<x * 2 <<std::endl;
	#if 0
		std::cout<<"hello world"<<std::endl;
	#endif
	return (0);
}
